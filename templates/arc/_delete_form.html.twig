<form method="post" action="{{ path('app_arc_delete', {'id': arc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arc.id) }}">
    <button class="btn">Delete</button>
</form>
