{% extends 'base.html.twig' %}

{% block title %}Arc{% endblock %}

{% block body %}
    <h1>Arc</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arc.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ arc.type }}</td>
            </tr>
            <tr>
                <th>Geoloc</th>
                <td>{{ arc.geoloc }}</td>
            </tr>
            <tr>
                <th>UserId</th>
                <td>{{ arc.userId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_arc_index') }}">back to list</a>

    <a href="{{ path('app_arc_edit', {'id': arc.id}) }}">edit</a>

    {{ include('arc/_delete_form.html.twig') }}
{% endblock %}
